#include <cstdlib>
#include <iostream>
#include <fstream>

using namespace std;

class MainMenu
{
public:
    MainMenu &mainprompt();
    // MainMenu &parkingPrompt(admins admins, manages manages, Users users, Tickets ticket1);
    // MainMenu &mainLogin(admins admins, manages manages, Users users, Tickets ticket1);
    // MainMenu &adminmenu(admins admins, manages manages, Users users, Tickets ticket1);
    // MainMenu &managerMenu(admins admins, manages manages, Users users, Tickets ticket1);
    // MainMenu &userMenu(admins admins, manages manages, Users users, Tickets ticket1);
};
